const GIFS_URL = "/api/gifs/search";

const USER_URL = "/api/users";

const LOGIN_URL = "/api/users/login";

const LOGOUT_URL = "/api/users/logout";

const LOGIN_PATHS = ["/login", "/register"];

const REDIRECT_ERROR_CODES = [401, 422, 409, 404];

const OFFSET = 12;

export {
  GIFS_URL,
  USER_URL,
  LOGIN_URL,
  LOGOUT_URL,
  LOGIN_PATHS,
  REDIRECT_ERROR_CODES,
  OFFSET
};
