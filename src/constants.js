const GIFS_URL = "/api/gifs/search";

const USER_URL = "/api/users";

const LOGIN_URL = "/api/users/login";

const LOGIN_PATHS = ["/login", "/register"];

const REDIRECT_ERROR_CODES = [401, 422];

export { GIFS_URL, USER_URL, LOGIN_URL, LOGIN_PATHS, REDIRECT_ERROR_CODES };
